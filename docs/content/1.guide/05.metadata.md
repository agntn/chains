---
title: Metadata
icon: i-lucide-sigma
description: Every field on a chain class. Which ones may be missing and why. Where each decimals value was checked.
---

## The fields

| Field        | Type        | Description                    |
| ------------ | ----------- | ------------------------------ |
| `key`        | `ChainKey`  | Canonical class key            |
| `name`       | `string`    | Display name                   |
| `symbol`     | `string`    | Native token symbol            |
| `decimals`   | `number?`   | Native currency decimal places |
| `type`       | `ChainType` | Blockchain family              |
| `bip44`      | `number?`   | BIP-44 / SLIP-0044 coin type   |
| `chainId`    | `string?`   | EVM chain ID in hexadecimal    |
| `caip2`      | `string?`   | CAIP-2 identifier              |
| `explorer`   | `string`    | Block explorer base URL        |
| `rpcDefault` | `string?`   | Default public RPC endpoint    |

`key` is read off the class, `chain.key` and `Bitcoin.key` are the same string. `type` is the family the class belongs to: `evm`, `utxo`, `solana`, `stellar`, `xrpl`, `move`, `ton`, `tron`, `octra`, `arweave` or `monero`. `EVM` and `Move` set it for their subclasses, every other chain declares it itself.

Optional fields stay empty when the chain has no registered value. Octra has no BIP-44 coin type and no CAIP-2 namespace, so both are `undefined` rather than invented. eCash and Decred have a coin type but no CAIP-2 identifier. Only EVM chains carry a `chainId`, and only some chains ship a public RPC worth defaulting to. The tools print an absent coin type or CAIP-2 as `none`, because a value that simply vanishes reads as "not shown" and invites the caller to supply one from memory.

`chainId` and the reference in `caip2` encode the same number twice, so a test checks them against each other. Linea shipped a testnet id against a mainnet CAIP-2 until that test existed.

## Decimals

`decimals` is the exponent in `1 whole native unit = 10^decimals base units`. It belongs to the currency, not the address family. eCash is the useful counterexample: XEC has two decimal places despite sharing Bitcoin's UTXO ancestry, and assuming eight for every UTXO chain would get XEC amounts badly wrong. Avalanche here means AVAX on the C-Chain; this metadata doesn't describe its X and P chains.

```ts
create("bitcoin").decimals; // 8
getChain("xec").decimals; // 2
```

This isn't a token's precision or a UI rounding preference, and an RPC may already return amounts in whole units. Every built-in chain declares `decimals`. Custom classes may leave it `undefined`, which means unknown, never a default of 18. `0` is a valid value for an indivisible currency. `chains info` and `chains_lookup` print `unknown` when the field is absent, `--json` omits it.

These references were checked on 2026-09-05, Arc on 2026-09-18, Dogecoin on 2026-09-23, Bitcoin Cash, Bitcoin SV and Bitcoin Gold on 2026-09-24. Tests pin the values independently of the classes, and no network request is needed at runtime or during tests.

| Chain | Decimals | Reference |
| --- | ---: | --- |
| Ethereum, Base, Arbitrum, Optimism, Polygon, BNB Chain, Avalanche C-Chain, Fantom, Gnosis, Linea, zkSync, Scroll, Berachain, Arc | 18 | [`ethereum-lists/chains`](https://github.com/ethereum-lists/chains/tree/master/_data/chains), `nativeCurrency.decimals` in `eip155-{1,8453,42161,10,137,56,43114,250,100,59144,324,534352,80094,5042}.json`; on Arc that is native USDC, the ERC-20 view of the same balance has 6 |
| Bitcoin | 8 | [Bitcoin Core `COIN`](https://github.com/bitcoin/bitcoin/blob/master/src/consensus/amount.h), 100,000,000 satoshis |
| Litecoin | 8 | [Litecoin `COIN`](https://github.com/litecoin-project/litecoin/blob/master/src/amount.h), 100,000,000 |
| Pepecoin | 8 | [Pepecoin `COIN`](https://github.com/pepecoinppc/pepecoin/blob/master/src/amount.h), 100,000,000; native PEP, not an ERC-20 token |
| eCash | 2 | [eCash build documentation](https://e.cash/build), 100 satoshis per XEC |
| Cardano | 6 | [Cardano denominations](https://docs.cardano.org/about-cardano/new-to-cardano/what-is-a-cryptocurrency), 1,000,000 lovelace per ADA |
| Solana | 9 | [Solana account structure](https://solana.com/docs/core/accounts/account-structure), 1,000,000,000 lamports per SOL |
| Stellar | 7 | [Stellar asset precision](https://developers.stellar.org/docs/learn/fundamentals/stellar-data-structures/assets), 10,000,000 stroops per XLM |
| XRP Ledger | 6 | [XRP currency format](https://xrpl.org/docs/references/protocol/data-types/currency-formats), 1,000,000 drops per XRP |
| Aptos | 8 | [AptosCoin initialization](https://github.com/aptos-labs/aptos-core/blob/main/aptos-move/framework/aptos-framework/sources/aptos_coin.move), `decimals = 8` |
| Sui | 9 | [SUI module](https://github.com/MystenLabs/sui/blob/main/crates/sui-framework/packages/sui-framework/sources/sui.move), 1,000,000,000 mist per SUI |
| TON | 9 | [TON Core conversion](https://github.com/ton-org/ton-core/blob/master/src/utils/convert.ts), 1,000,000,000 nano units per TON |
| TRON | 6 | [TRX denominations](https://developers.tron.network/docs/token-standards-trx), 1,000,000 sun per TRX |
| Octra | 6 | [Octranomics](https://docs.octra.org/oct-docs/octranomics), 1,000,000 operational units per OCT |
| Arweave | 12 | [Arweave debugging guide](https://docs.arweave.org/developers/development/debugging), 1,000,000,000,000 Winston per AR |
| Monero | 12 | [Monero configuration](https://github.com/monero-project/monero/blob/master/src/cryptonote_config.h), `CRYPTONOTE_DISPLAY_DECIMAL_POINT = 12` |
| Decred | 8 | [Decred `AtomsPerCoin`](https://github.com/decred/dcrd/blob/master/dcrutil/const.go), 100,000,000 atoms |
| Dogecoin | 8 | [Dogecoin `COIN`](https://github.com/dogecoin/dogecoin/blob/master/src/amount.h), 100,000,000 |
| Bitcoin Cash | 8 | [Bitcoin Cash Node `COIN`](https://gitlab.com/bitcoin-cash-node/bitcoin-cash-node/-/blob/master/src/amount.h), 100,000,000 satoshis |
| Bitcoin SV | 8 | [SV Node `COIN`](https://github.com/bitcoin-sv/bitcoin-sv/blob/master/src/amount.h), 100,000,000 satoshis |
| Bitcoin Gold | 8 | [Bitcoin Gold `COIN`](https://github.com/BTCGPU/BTCGPU/blob/master/src/amount.h), 100,000,000 satoshis |

## Coin types

`bip44` is the SLIP-0044 coin type, the number that goes into `m/44'/coin'/…`. All fourteen EVM chains repeat `60`, Ethereum's, which is what wallets actually derive with. Decred is `42`, not its legacy `20`. Octra has none registered and the field says so instead of borrowing one. Deriving keys from it belongs in [`@agntn/keys`](https://github.com/agntn/keys), not here.
