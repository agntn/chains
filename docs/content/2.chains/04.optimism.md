---
title: Optimism
icon: i-token-op
description: OP Mainnet, chain 10. The explorer is optimistic.etherscan.io.
---

::chain-facts{chain="optimism"}
::

Chain 10, the original OP Stack chain. The alias is `op`, two letters people already use.

## Address format

The EVM format, shared by every EVM chain here: `0x` and 40 hex digits, any case. An address that fits Optimism fits the other thirteen EVM chains too, and [identify](/guide/identify) says so rather than picking one. The EIP-55 checksum, the one carried in letter case, is verified where there is one: mixed case has to match the hash, and an address in one case throughout passes, since it carries no checksum.

```ts
import { getChain } from "@agntn/chains";

const chain = getChain("op");
chain.assertAddress("0x1f9840a85d5aF5bf1D1762F925BDADdC4201F984"); // returned unchanged
chain.chainId; // "0xa"
chain.caip2; // "eip155:10"
```

## Spellings

`optimism`, `op`, `Optimism`. The key is `optimism`, the rest resolve through `getChain`.
