---
title: Arbitrum One
icon: i-token-arbitrum-one
description: Arbitrum One, chain 42161. Nova is not registered.
---

::chain-facts{chain="arbitrum"}
::

This is Arbitrum One, chain 42161. Arbitrum Nova is a different chain ID and isn't in the registry.

## Address format

The EVM format, shared by every EVM chain here: `0x` and 40 hex digits, any case. An address that fits Arbitrum One fits the other thirteen EVM chains too, and [identify](/guide/identify) says so rather than picking one. The EIP-55 checksum, the one carried in letter case, is verified where there is one: mixed case has to match the hash, and an address in one case throughout passes, since it carries no checksum.

```ts
import { getChain } from "@agntn/chains";

const chain = getChain("arb");
chain.assertAddress("0x1f9840a85d5aF5bf1D1762F925BDADdC4201F984"); // returned unchanged
chain.chainId; // "0xa4b1"
chain.caip2; // "eip155:42161"
```

## Spellings

`arbitrum`, `arb`, `arb1`, `Arbitrum One`. The key is `arbitrum`, the rest resolve through `getChain`.
